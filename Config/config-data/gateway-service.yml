server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
      - id: salesmanagemen
        uri: lb://salesmanagement-service:8082
        predicates:
        - Path=/api/**

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8088/eureka           


